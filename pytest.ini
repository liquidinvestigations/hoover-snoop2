[pytest]
DJANGO_SETTINGS_MODULE = testsuite.settings
norecursedirs = factory
# 40 min for pytest-timeout
timeout = 2400

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

log_level = INFO
# log_cli = true
